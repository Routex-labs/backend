<!-- ## 쓰는 규칙 두 가지
     1. **각 문단·불릿은 짧게(2~3줄).** 팀원이 자기 말로 그대로 옮길 수 있는 길이여야
        합니다. 길어진다는 것은 잘 못 쓴 게 아니라 **여기 쓸 내용이 아니라는 신호**입니다
        — 그때는 아래 2번대로 md로 뺍니다.
     2. **문제 분석과 접근 방식은 `docs/`의 md로 정리하고, 이 PR에는 결론과 링크만.**
        PR 본문은 머지되면 코드와 함께 갱신되지 않지만 문서는 갱신됩니다. 판단 근거를
        본문에만 적어 두면 6개월 뒤 "왜 이렇게 했는지"를 아무도 찾지 못합니다. -->

<!-- 맨 위 한두 문장으로 "무엇을 왜 했는지"를 먼저 적습니다. 제목만으로는 범위를 알 수
     없고, 리뷰어가 아래 다섯 절을 읽을지 여기서 정합니다. -->

## 1. 해결할 문제

<!-- 기존에 어떤 문제가 있었는지, 그리고 이 PR이 머지되면 무엇이 "완료"인지.

     체크박스를 쓰는 이유가 있습니다 — **기존 문제는 체크하지 않고**(문제는 달성 대상이
     아니라 사실입니다) **완료 조건만 충족될 때 체크합니다.** 그러면 리뷰어가 두 목록의
     체크 상태만 보고 "이 PR이 스스로 세운 기준을 지켰는지"를 먼저 확인할 수 있습니다. -->

**기존 문제**

- [ ]

**완료 조건**

- [ ]

## 2. 접근 방법

<!-- **여기는 결론만 적는 자리입니다.** 어떤 구조/방식을 골랐는지를 축마다 한 줄씩,
     **키워드 — 설명** 꼴로 씁니다. 리뷰어가 관심 있는 축부터 읽을 수 있습니다.

     검토 과정·버려진 대안·실패 조건처럼 길어지는 것은 **`docs/`의 md로 정리하고 아래에
     링크만 겁니다.** 같은 근거를 PR 본문과 문서 양쪽에 적지 않습니다 — 반드시 한쪽이
     먼저 낡습니다. 버린 대안은 특히 문서에 남깁니다. 나중에 같은 대안이 다시 제안될 때
     그 줄이 답이 됩니다. -->

- **** —

<!-- 판단 근거를 정리한 문서. 새로 쓴 문서든 갱신한 문서든 여기 겁니다. -->

자세한 판단 근거와 버려진 대안은 [문서 제목](docs/경로.md)에 적어 뒀습니다.

## 3. 변경 범위

<!-- `경로 — 무엇을 바꿨는지` 꼴로 적습니다. 파일 목록은 diff에 이미 있으므로, 여기서는
     "그 파일에서 무엇이 달라졌는가"를 한 줄로 말해 주는 것이 값입니다.

     **건드리지 않은 영역**은 비워 두지 않습니다. 관련돼 보이는데 손대지 않은 곳을 적어
     두면 리뷰어가 "왜 여기는 안 고쳤지"를 묻느라 시간을 쓰지 않습니다. -->

**바꾼 영역**

- ` ` —

**건드리지 않은 영역**

-

## 4. 검증 내용

<!-- 돌린 검사와 실제로 확인한 시나리오. 숫자를 적습니다(테스트 N건 통과).

     **확인하지 못한 것은 지우지 말고 `- [ ]`로 남깁니다.** 빠뜨린 것과 못 한 것은 다르고,
     남겨 두면 리뷰어가 그 자리를 대신 봐 줄 수 있습니다. -->

- [ ]

## 5. 남은 위험

<!-- 없으면 각 항목에 "없음"이라고 명시합니다. 비워 두면 "점검했는데 없다"인지
     "아직 안 봤다"인지 구분되지 않습니다. -->

**미확인 영역**

<!-- 온전히 확인하지 못한 채로 머지되는 것. -->

-

**배포 후 관찰 항목**

<!-- 지금은 문제가 아니지만 시간이 지나거나 실사용에서 드러날 수 있는 것. -->

-

**방치된 코드/문서**

<!-- **이 항목의 정상값은 "없음"입니다.**

     더 이상 참조되지 않게 된 코드와 낡아진 문서는 여기 적는 것이 아니라 **같은 PR에서
     지우고 고칩니다**(제거·문서 수정은 별도 커밋 — CONTRIBUTING.md). 적어 두는 순간 그것은
     "다음에 할 일"이 되고, 다음은 오지 않습니다.

     그러니 여기에 무언가 적혀 있다면 그것은 **뒷정리를 미뤘다는 신고**입니다. 지울 수
     없는 경우(범위가 크거나 결정이 필요, 다른 브랜치가 쓰는 중)만 그 이유와 함께
     남깁니다. -->

- 없음
